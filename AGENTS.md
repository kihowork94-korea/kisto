# AGENTS.md — 키스토(KISTO)

작업을 시작하기 전에 순서대로 읽는다.

1. **`HANDOFF.md`** — 인수인계: 실행 방법, 코드 지도, 검증된 수치, 남은 일, 밟은 함정
2. **`CLAUDE.md`** — 프로젝트 원칙(지켜야 할 설계 결정)과 작업 기록. 에이전트 종류와 관계없이 이 파일의 규칙을 따른다
3. `README.md` — 사용법

## 작업 규칙

- 답변과 문서는 한국어로. 사용자는 KIST 신입 연구원이고 공모전 서류 마감은 **2026-10-02**다.
- 코드 주석·문서 문체는 주변 코드에 맞춘다 (한국어 주석, "왜"를 적는다).
- 바뀐 상태는 `CLAUDE.md`의 "현재 상태"와 `HANDOFF.md`에 반영한다.
- 커밋은 사용자가 요청할 때만. `.env`와 `data/`의 개인 데이터는 커밋하지 않는다.
- API 키는 채팅으로 받지 않는다 — 사용자가 `.env`에 직접 넣는다.
- 사용자의 예전 사업계획서 hwpx와 Unity 빌드(`AIWAIFU_OUTPUT`)는 저장소에 넣지 않는다 (개인정보·API 키 포함).
- 모델 호출 테스트는 동시에 여러 개 돌리지 않는다 (사용자 Claude 사용 한도).
