fastapi
uvicorn[standard]
pydantic
python-dotenv
anthropic
openai
google-genai
# 분석 샌드박스에서 생성 코드가 쓰는 라이브러리 (코드가 직접 설치하지 않도록 미리 넣는다)
numpy
scipy
pandas

# 에셋 전처리 스크립트용 (scripts/prep_lab_assets.py). 백엔드 동작에는 필요 없다
pillow
