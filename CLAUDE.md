# 키스토 (KISTO) — 작업 이어받기 지침

작업을 시작하기 전에 이 파일과 `README.md`를 먼저 읽는다. 다른 에이전트(Codex 등)로 이어받는 경우의
요약·실행 방법·남은 일·함정은 **`HANDOFF.md`**에 있다 (Codex는 `AGENTS.md`를 먼저 읽는다).

## 무엇을 만드는가

2026 KIST AIX 성과 공유회 출품작. 사용자는 KIST 신입 연구원으로, 코딩 역량은
높지만 연구 도메인 경험은 쌓는 중이다. 본인이 겪은 문제에서 출발했다:
"배경지식이 부족해 연구 주제 설정과 논문 작성이 어려웠고, 파편화된 AI 챗이 아니라
진짜 선배/동료 같은 에이전트가 필요했다."

**컨셉**: 바탕화면 캐릭터 위젯 하나가 유일한 입력창. 그 뒤에서 멀티에이전트 연구
파이프라인(문헌·가설 → 분석 → 교차검증 → 집필)이 돌지만, 사용자에게는 "한 명의
동료 과학자와 대화하는" 것처럼 보여야 한다.

## 공모전 조건

- 서류 마감 **2026-10-02(금)**, 1단계 결과 ~10-08, 발표 10-28 (10분 발표 + 5분 Q&A)
- 심사배점: **창의성 40** / 업무활용성 30 / 확산가능성 30
- 신청서 항목: 추진배경 및 목적 / AI 활용 내용 / 주요 성과 및 개선 효과 / 확산 가능성 /
  첨부자료(github repo, 시연영상 가능)
- 성과를 "시간 단축 등 실측 before/after"로 보여주는 게 중요하다

## 지켜야 할 설계 결정

1. **실제 API를 연동했다고 주장하지 않는다.** Google AI Co-Scientist, FutureHouse
   Kosmos는 공개 API가 없다. "이들의 멀티에이전트 *패턴*을 참고해 자체 재구현했다"로
   표현한다.
2. **교차검증은 결과를 만든 모델과 다른 회사 모델로.** Google Co-Scientist조차
   Gemini 계열 안에서만 검증하므로 이게 핵심 차별점이다.
3. **"주제별 대화 폴더" UI는 만들지 않는다.** Claude/ChatGPT Projects와 똑같아져서
   차별성이 사라진다. 대신 각 주제가 5단계 중 어디까지 왔는지 보여주는 게임식
   **연구 로드맵**으로 노출한다. 단계는 저장된 실데이터로만 판정한다.
4. **사용자에게 내부 구조를 드러내지 않는다.** 페르소나 레이어가 모든 응답을 키스토
   말투로 통일한다. 예외는 두 가지뿐이다 (2026-09-18 심사 대비로 추가):
   - **검토 노트**: 교차검증 결과는 답변과 분리해 접힌 노트로 보여준다 (모델 이름은 안 보임)
   - **발표 모드**(트레이 메뉴, 기본 꺼짐): 답변마다 에이전트·회사 모델·소요 시간 흐름을 보여준다.
     심사위원이 핵심 차별점(이종 모델 교차검증)을 눈으로 확인할 수 있게 하기 위한 것이다.
5. **집필 모듈은 대필하지 않는다.** 먼저 사용자 해석을 묻고 그걸 반영해 초안을 쓴다
   (연구윤리 + 학습효과).
6. 멀티 LLM은 2주 MVP에서 전 모듈에 얽지 않는다. 한 지점(분석=OpenAI, 검증=Gemini)
   에서 실제로 증명하고 나머지는 "설정으로 교체 가능"으로 설명한다.

## 현재 상태 (2026-09-18 기준)

완료:
- 모델 어댑터 4종 (Claude API / Claude Code 하네스 / OpenAI / Gemini) + 자동 폴백
- 라우터, 페르소나 레이어, 모듈 A/B/C/D, JSON 메모리
- `KISTO.md` 사용자 지침 파일 (재시작 없이 반영)
- 연구 로드맵 API(`/threads/{session_id}`) + 테스트용 프론트 렌더링 확인
- API 키 없이 하네스만으로 서버 부팅 확인
- **실제 LLM 응답 엔드투엔드 테스트 통과** (하네스 단독, 세션 `e2e-2026-09-17`):
  주제 탐색 → 분석(코드 생성·실행·t검정·가설과 연결한 해석) → 교차검증 기록 →
  집필(먼저 해석을 되묻고, 답을 반영한 서론 초안)까지 이어졌고 로드맵이
  0 → 40% → 80% → 100%로 실제 데이터 기준으로 채워지는 것을 확인했다.
  집필 모듈이 대필하지 않고 되묻는 동작도 실제로 확인됨.
- 테스트 중 발견해 고친 것:
  - 하네스 어댑터가 cp949로 CLI 출력을 읽어 stdout이 통째로 `None`이 되던 문제
    (한국어 Windows에서는 무조건 재현. UTF-8 고정으로 해결)
  - 샌드박스가 PATH의 `python`을 찾던 문제 → 백엔드와 같은 인터프리터를 쓰도록 변경,
    자식 프로세스 출력도 UTF-8로 고정 (한글 print가 깨져 해석 단계로 넘어가던 문제)
  - 하네스 타임아웃 60초 → 180초 (분석 결과처럼 긴 입력의 검증 호출이 항상 실패했음)
  - 라우터 `auto` 분기에 분석 이후 집필로 가는 경로가 없어서 집필 요청이
    research_coach로 되돌아가던 문제

- (2026-09-18) 집필 응답에서 초안이 사라지던 문제 해결: 초안은 페르소나로 다시 쓰지 않고
  원문 그대로 보여준다. 검증 결과는 답변과 분리된 `review`(검토 노트)로 돌려준다.
- (2026-09-18) API 어댑터를 현재 SDK에 맞게 정리: Claude는 `claude-opus-5` + thinking 블록을
  건너뛰는 텍스트 추출 + 서버 측 fallback, OpenAI는 Responses API(codex 계열은 Chat
  Completions로 호출 불가), Gemini는 `system_instruction` 사용. 모델은 `.env`의
  `ANTHROPIC_MODEL`/`OPENAI_MODEL`/`GEMINI_MODEL`로 교체 가능. 키가 없어서 실제 호출 대신
  가짜 서버로 요청·응답 형태만 검증했다.
- (2026-09-18) `/chat` 응답에 `verified_by` 추가, 테스트 프론트의 디버그 표시는
  `?debug=1`일 때만 보인다 (설계 결정 4).
- (2026-09-18) `scripts/e2e.py` — 전체 흐름 자동 점검 + 단계별 소요 시간 기록
  (`data/e2e_result.json`). 데모 시나리오(신생아 rPPG) 하네스 단독 기준 4단계 전부 통과, 총 414초.
- (2026-09-18) 잡담 경로에 키스토 정체성 프롬프트 추가 — 없을 때는 하네스(Claude Code)가
  "웹 검색, 파일 편집" 같은 자기 기능을 소개해버렸다.
- (2026-09-18) **바탕화면 캐릭터 위젯 (`widget/`, Electron)** 완성. 친구가 만들어 준 Unity
  빌드(`Desktop\AIWAIFU_OUTPUT`, 원본 프로젝트 없음)에서 캐릭터 대기 영상만 꺼내
  `widget/character/idle.mp4`로 쓴다. Unity 빌드 자체는 쓰지 않는다 (OpenAI 직접 호출 +
  API 키 하드코딩 — 저장소에 절대 넣지 않는다).
  검은 배경 WebGL 실시간 투명 처리, 투명 영역 클릭 통과, 드래그 이동, 대화창 + 로드맵,
  생각 중 말풍선(경과 시간), 단계 클리어 연출, 트레이 메뉴, `Ctrl+Shift+K`.
  Electron으로 띄워 캡처해서 동작을 확인했다 (주제 탐색 1회 → 로드맵 40% + 클리어 연출).
- (2026-09-18) `run.cmd`/`run.ps1` — 더블클릭으로 백엔드(숨김) + 위젯 실행.
  claude CLI 경로를 자동으로 찾는다.
- (2026-09-18) 데모 시나리오로 돌려보고 응답 품질 문제 수정:
  - 분석 해석에 원래 요청을 같이 넘김 (없으면 "이 데이터가 성인인지 모른다"고 헛짚음)
  - 집필의 "해석 묻기" 단계는 페르소나·검증을 거치지 않음 (페르소나가 질문에 스스로 답해버림).
    질문 프롬프트 자체를 키스토 말투로 작성
  - 분석 코드·초안이 주어지지 않은 판정 기준(예: "임상 허용 ±10bpm")이나 인용을 지어내지 않도록
    프롬프트 보강
  - **분석 샌드박스의 생성 코드가 백엔드 가상환경에 numpy/scipy를 직접 pip 설치한 적이 있다.**
    numpy/scipy/pandas를 requirements에 넣고, 샌드박스 환경변수 `PIP_NO_INDEX=1`로 설치를 막았다.
    어디까지나 완화책이고 진짜 격리는 아니다 (배포 전 컨테이너 필요).
- (2026-09-18) **심사위원 관점 점검 → 차별화 기능 구현** (`docs/judging_review.md`에 근거와 예상 Q&A):
  - 모든 모듈에 스레드별 연구 맥락(`thread_brief`) 주입 — 전에는 모듈마다 현재 메시지만 받았다.
    실제 기록이 없으면 맥락을 넘기지 않는다 (빈 맥락을 주면 "지난번에 말씀드린"을 지어냄)
  - 실제 논문 검색 `literature.py` (PubMed → OpenAlex). 라우터가 영어 검색어를 같이 뽑는다.
    [번호] 인용만 허용, **참고문헌 목록은 코드가 붙인다**. 초안도 이 목록으로만 인용,
    검증 에이전트는 인용 문장과 초록을 대조한다
  - 검토 노트(`review`) + 발표 모드(`trace`: 에이전트·회사 모델·소요 시간, `tracing.py`)
  - CSV 첨부(`POST /upload`, 위젯 📎·끌어다 놓기) → 분석 코드가 파일을 직접 읽음
  - 연구노트 내보내기(`GET /threads/{s}/{i}/note`, 위젯 📓 → `문서\키스토 연구노트`)
  - 개인정보 자동 가림(`privacy.py`, 메시지·첨부 파일의 주민번호·전화번호·이메일)
  - 모델 호출 실패는 500 대신 503 + 이유 (Claude Pro 사용 한도에 실제로 걸렸다 — 시연은 API 키로)
  - 페르소나가 연구 코치 답변을 "사용자가 정리해 준 글"로 착각해 고마워하던 문제 → 프롬프트로 해결
  - 이 상태로 데모 시나리오 4단계 통과 (435초, 실제 논문 [3] Villarroel 2019를 초안까지 인용)
- (2026-09-18) `scripts/verifier_eval.py` — 오류를 심어 둔 결과 10건 + 정상 2건으로 검증 에이전트
  탐지율 측정. 하네스(Claude) 단독 결과: **10/10 탐지, 오탐 0/2** — 사례가 쉬워 천장 효과가 있으니
  이종 모델 비교 전에 더 어려운 사례를 추가하는 게 좋다. 키를 넣은 뒤 `--verifier gemini` 등으로 비교한다.

- (2026-09-18) **"나만의 연구실" 컨셉 + 게임식 연구실 화면** (사용자 결정). 사용자=책임연구원(PI),
  키스토=연구실 매니저, 에이전트=자리가 있는 연구원(서재·실험대·노트북·검토 보드).
  - 위젯 🏠 버튼/트레이 → `widget/lab.html` 연구실 창. 자리를 누르면 그 연구원의 일이 서랍에 열린다
  - 게임 요소(책·시험관·메모지 수, ❗ 퀘스트 표시, 별·연구실 레벨)는 전부 실데이터로 계산 (`backend/lab.py`)
  - 역할 부여: 연구원별 담당 모델·지침 (`GET/PUT /lab/roles`, `data/lab_settings.json`).
    검토위원은 같은 회사로 지정해도 다른 회사 모델이 있으면 그쪽으로 배정 (설계 결정 2 유지)
  - 모든 검토 결과를 스레드 `reviews`에 기록 (검토 보드용). 로드맵 4단계 판정은 여전히 분석 검증만
  - 위젯 캐릭터 위 버튼 막대(💬 대화 · 🏠 연구실 · ➖ 숨기기 · ✕ 종료) + 대화창 닫기 ✕ + 첫 실행 안내 말풍선 —
    실제로 띄워 보니 대화 방법(캐릭터 클릭)과 끄는 방법(트레이 메뉴)을 알 수 없다는 피드백이 있었다
  - 바탕화면 **키스토** 바로가기 → `run.cmd -Chat -Lab` (대화창 펼침 + 연구실)
  - `run.cmd -Lab`: 위젯과 연구실 창을 같이 연다. 연구실 창은 Windows 포커스 제한 때문에 뒤로 숨어서
    `bringToFront`로 잠깐 최상단에 올렸다 내린다
  - 연구실 **📊 대시보드** 탭 (`GET /lab/session/{s}/dashboard`, `lab.dashboard()`): 지표 타일, 단계 퍼널,
    회사별 모델 호출(100% 막대, 회사별 색 고정), 연구원별 업무·시간, 프로젝트 표, 활동 타임라인.
    대화마다 `activity`(시각·모듈·소요 시간·단계별 모델)를 스레드에 남겨서 계산한다
  - 기다리는 동안 위젯 말풍선에 지금 일하는 연구원 표시 (`GET /progress/{s}`, `tracing.LIVE`)
  - 교차검증과 페르소나 다듬기를 병렬 실행 — 주제 탐색 답변 107~145초 → 78초
  - **보안**: 전에는 CORS `*`라 아무 웹사이트나 로컬 백엔드를 불러 기록을 읽고 `/chat`으로 분석 코드
    실행까지 유도할 수 있었다. 토큰(`data/.kisto_token`) + Origin 허용 목록 + Host 검사로 막았다
  - 그림은 벡터 임시본. **사용자가 이미지 에셋을 나중에 준다** → `widget/lab-assets/`에 넣고
    `lab.json`에 적으면 코드 수정 없이 교체 (규격: `lab-assets/README.md`)

아직 안 한 것:
- 연구실 이미지 에셋 적용 (사용자가 제작 중). 받으면 `lab-assets/lab.json`에 등록하고 위치만 맞춘다.
  생성용 프롬프트는 `widget/lab-assets/prompts/`에 종류별로 준비해 뒀다 (2026-09-21).
  폴더를 통째로 생성 AI에 주면 한 장만 만들고 끝나서, **파일 하나 = 이미지 한 장**인
  복붙용 `prompts/plain/`과 지시문 `prompts/HOW_TO_ORDER.md`를 따로 뒀다.
  받은 그림은 `scripts/prep_lab_assets.py`(흰 배경 제거·여백 자르기·규격 크기)로 다듬고
  `scripts/check_lab_assets.py`(크기·비율·투명도 점검)로 확인한다. 둘 다 실제로 돌려 봤다.
  선택 항목인 UI 아이콘·연구원 초상까지 넣어 주면 그때 코드에 붙인다.
- 교차검증을 **실제 이종 모델로** 돌려본 적은 아직 없다. 지금은 provider가
  `claude_harness` 하나뿐이라 Claude가 Claude를 검증하는 상태다.
  데모·발표 전에 OpenAI 또는 Gemini 키를 넣고 `scripts/e2e.py`의 "이종검증" 열이 O인지
  확인해야 한다 (설계 결정 2번).
- 신청서: **hwpx 제출본은 `docs/KISTO_AIX_신청서.hwpx`** (`scripts/build_application_hwpx.py`로 생성, 7쪽).
  서식은 사용자의 예전 사업계획서(`Desktop\창업성공패키지_...hwpx`, 개인·회사 정보가 있으니 저장소에 넣지
  않는다)에서 스타일만 빌렸다. 한글 COM 자동화로 열어 PDF로 확인함. 주의: 템플릿 header.xml의
  `secCnt`(구역 수)를 1로 고치지 않으면 한글이 파일을 못 연다. 원본 공모전 신청서 양식 파일은 받지 못해서
  목차는 이 파일의 '공모전 조건'에 적힌 항목으로 잡았다 — 실제 양식을 받으면 맞춰 볼 것.
  마크다운 초안은 `docs/application_draft.md`. **실측 before/after 수치**와 이종 모델
  실증 결과 칸이 비어 있다 (사용자 측정 필요).
- 시연영상: 대본은 `docs/demo_scenario.md`. 촬영은 API 키를 넣은 뒤에 사용자가 한다.
- 발표자료 (10-28 발표용, 1단계 결과 10-08 이후에 만들어도 된다)

결정된 것:
- **데모 주제: 신생아·영유아 대상 비침습 연구** (2026-09-18 사용자 결정). 시나리오는
  카메라 기반 비접촉 심박 측정(rPPG) vs 심전도(ECG) 일치도 분석이고, 데이터는
  **시연용 가상 데이터**다 (실측값처럼 발표하지 않는다). `KISTO.md` 연구 분야도 이걸로 설정.
- **OpenAI·Gemini API 키는 사용자가 맨 마지막에 넣는다.** 다른 작업이 모두 끝나면 그때
  키를 요청하고, `scripts/e2e.py`로 이종 모델 교차검증을 실증한다.
- 캐릭터 영상은 지금 있는 `idle.mp4` 하나가 전부다 (추가 영상 없음). 생각 중/축하는
  CSS 효과로 표현한다.
- Hailuo AI 워터마크는 `crop.bottom: 0.058`로 잘라서 가린다 (부츠 밑창만 살짝 잘림).

## 개발 환경 메모 (이 PC 기준)

- Python은 설치 프로그램이 아니라 공식 NuGet 배포판을 풀어 `C:\Users\user\Python314`에
  넣었다 (PATH에 없음). 프로젝트는 `.venv`를 쓴다: `.venv\Scripts\activate`
- `claude` CLI는 데스크톱 앱 번들에 있고 PATH에 없다. 서버를 띄울 때만 경로를 추가한다:
  `C:\Users\user\AppData\Local\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude-code\<버전>`
- Node.js 24 LTS도 같은 이유로 zip 배포판을 `C:\Users\user\nodejs`에 풀었다 (PATH에 없음).
- 하네스 단독 구성에서는 `/chat` 한 번에 CLI를 3~4회 띄우므로 응답에 1~3분 걸린다.
  데모 때는 API 키를 쓰는 편이 안전하다.

## 참고한 선례 (신청서·발표 근거)

- Google AI Co-Scientist (2025.2, Nature 게재) — 가설 생성 주 단위→일 단위, 습식실험 검증
- FutureHouse Kosmos — 6개월 연구를 하루에, 정확도 약 80%
- Sakana AI Scientist v1/v2 — 아이디어→실험→논문→리뷰 전 과정 자동화
- AutoResearch AI 5단계 프레임워크 — 로드맵 5단계의 근거
- 넥센타이어 사내 AI 공모전 — 현업 연구원이 직접 만든 실무 통합 도구가 1위
- Microsoft AI Agents Hackathon 2025 우승작 Konveyor — 지식연속성 에이전트
- 기존 AI 데스크톱 펫(PetGPT 등)과의 차이: 그쪽은 "캐릭터 옷을 입은 단일 LLM 챗봇",
  키스토는 "캐릭터 뒤에 연구 파이프라인과 교차검증이 숨어 있는 구조"
